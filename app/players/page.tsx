"use client";
import Link from "next/link";
import {useEffect,useMemo,useState} from "react";
type Player={id:number;slug:string;name:string;position:string|null;notes:string|null;appearances:number;goals:number;minutes:number};
const U="https://uwhewuwnrcvrnclfzoge.supabase.co",K="sb_publishable_3qBGcpu8I6fytBGxdhJDNA_zOklTBeT",H={apikey:K,Authorization:`Bearer ${K}`};
function pg(v:string|null){const p=(v||"").toLowerCase();if(p.includes("goalkeeper"))return"Goalkeeper";if(p.includes("defender")||p.includes("defence"))return"Defender";if(p.includes("midfield"))return"Midfielder";if(p.includes("forward")||p.includes("striker"))return"Forward";return"Unknown"}
function yrs(n:string|null){const y=(n||"").match(/(?:19|20)\d{2}/g)||[];return y.length?`${y[0]}${y.length>1?` to ${y[y.length-1]}`:""}`:"Not recorded"}
function ps(n:string|null,s:string){if(!s)return true;const y=+s,a=((n||"").match(/(?:19|20)\d{2}/g)||[]).map(Number);return !!a.length&&y>=Math.min(...a)&&y<=Math.max(...a)}
export default function Players(){const [players,setPlayers]=useState<Player[]>([]),[loading,setLoading]=useState(true),[search,setSearch]=useState(""),[position,setPosition]=useState(""),[season,setSeason]=useState(""),[sort,setSort]=useState("name");useEffect(()=>{Promise.all([fetch(`${U}/rest/v1/players?select=id,slug,name,position,notes&order=name.asc`,{headers:H}).then(r=>r.json()),fetch(`${U}/rest/v1/player_career_totals?select=player_id,appearances,goals,minutes`,{headers:H}).then(r=>r.json())]).then(([p,t])=>{const m=new Map(t.map((x:any)=>[x.player_id,x]));setPlayers(p.map((x:any)=>({...x,appearances:Number(m.get(x.id)?.appearances||0),goals:Number(m.get(x.id)?.goals||0),minutes:Number(m.get(x.id)?.minutes||0)})))}).finally(()=>setLoading(false))},[]);const seasons=useMemo(()=>Array.from({length:93},(_,i)=>2024-i),[]);const filtered=useMemo(()=>players.filter(p=>(!search||p.name.toLowerCase().includes(search.toLowerCase()))&&(!position||pg(p.position)===position)&&ps(p.notes,season)).sort((a,b)=>sort==="apps"?b.appearances-a.appearances:sort==="goals"?b.goals-a.goals:sort==="position"?pg(a.position).localeCompare(pg(b.position)):sort==="recent"?yrs(b.notes).localeCompare(yrs(a.notes)):a.name.localeCompare(b.name)),[players,search,position,season,sort]);return <main className="archivePage"><header className="archiveNav"><Link className="brand" href="/">PARS<span>DATABASE</span></Link><Link href="/">← Home</Link></header><section className="archiveHero playerHero"><p className="eyebrow">THE PEOPLE BEHIND THE PARS</p><h1>Players</h1><p>Search, filter and rank Dunfermline Athletic players across the club's history.</p></section><section className="archiveContent"><div className="archiveToolbar"><div><h2>Player archive</h2><span className="resultCount">{loading?"Loading live records…":`${filtered.length} of ${players.length} players`}</span></div></div><div className="filterPanel"><label><span>Search</span><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search player name…"/></label><label><span>Position</span><select value={position} onChange={e=>setPosition(e.target.value)}><option value="">All positions</option><option>Goalkeeper</option><option>Defender</option><option>Midfielder</option><option>Forward</option><option>Unknown</option></select></label><label><span>Season played</span><select value={season} onChange={e=>setSeason(e.target.value)}><option value="">All seasons</option>{seasons.map(y=><option value={y} key={y}>{y}/{String(y+1).slice(-2)}</option>)}</select></label><label><span>Sort by</span><select value={sort} onChange={e=>setSort(e.target.value)}><option value="name">Name A to Z</option><option value="apps">Most appearances</option><option value="goals">Most goals</option><option value="position">Position</option><option value="recent">Most recent</option></select></label><button className="clearFilters" onClick={()=>{setSearch("");setPosition("");setSeason("");setSort("name")}}>Clear</button></div><div className="playerTable"><div className="playerTableHead"><span>Player</span><span>Position</span><span>Years active</span><span>Apps</span><span>Goals</span><span></span></div>{filtered.map(p=><Link className="playerTableRow" href={`/players/${p.slug}/`} key={p.id}><strong>{p.name}</strong><span className="positionPill">{pg(p.position)}</span><span>{yrs(p.notes)}</span><span>{p.appearances||"–"}</span><span>{p.goals||"–"}</span><b>→</b></Link>)}</div><p className="migrationNote"><strong>Statistics coverage:</strong> appearance and goal totals currently reflect migrated competitive season-stat records. 2000/01 is connected first, and totals will grow automatically as earlier and later seasons are imported.</p></section></main>}
