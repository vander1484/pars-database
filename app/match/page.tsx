"use client";

import Link from "next/link";
import { Suspense, useEffect, useState } from "react";
import { useSearchParams } from "next/navigation";
import ClubBadge from "../components/ClubBadge";

type Player = { number?: number; name: string; position?: string; captain?: boolean; };
type Lineup = { club_id: number; formation: string | null; manager_name: string | null; starting_xi: Player[]; substitutes: Player[]; };
type Goal = { player_id:number; player_name:string|null; player_slug:string|null; minute:number|null; own_goal:boolean; penalty:boolean };
type Match = { id: number; played_on: string | null; season_id: number; round: string | null; home_club_id: number; away_club_id: number; home_score: number | null; away_score: number | null; attendance: number | null; venue: string | null; notes: string | null; home: { name: string } | null; away: { name: string } | null; competition_season: { display_name?: string | null; competition: { name: string } | null; } | null; season: { label: string; slug: string } | null; };
const U = "https://uwhewuwnrcvrnclfzoge.supabase.co", K = "sb_publishable_3qBGcpu8I6fytBGxdhJDNA_zOklTBeT", H = { apikey: K, Authorization: `Bearer ${K}` };
function displayDate(value: string | null) { if (!value) return "Date not recorded"; return new Intl.DateTimeFormat("en-GB", { weekday: "long", day: "numeric", month: "long", year: "numeric" }).format(new Date(`${value}T12:00:00`)); }
function PlayerRow({ player }: { player: Player }) { return <div className="playerRow"><b>{player.number ?? ""}</b><span>{player.name}{player.captain ? " (c)" : ""}</span><small>{player.position ?? ""}</small></div>; }
function scorerName(name:string){const parts=name.split(",").map(x=>x.trim());return parts.length===2?`${parts[1]} ${parts[0]}`:name}
function MatchRecord() {
 const id=useSearchParams().get("id"),[match,setMatch]=useState<Match|null>(null),[lineups,setLineups]=useState<Lineup[]>([]),[goals,setGoals]=useState<Goal[]>([]),[nearby,setNearby]=useState<Match[]>([]),[loading,setLoading]=useState(true);
 useEffect(()=>{if(!id){setLoading(false);return}const select="id,played_on,season_id,round,home_club_id,away_club_id,home_score,away_score,attendance,venue,notes,home:clubs!matches_home_club_id_fkey(name),away:clubs!matches_away_club_id_fkey(name),competition_season:competition_seasons(display_name,competition:competitions(name)),season:seasons(label,slug)";fetch(`${U}/rest/v1/matches?id=eq.${id}&select=${select}`,{headers:H}).then(r=>r.json()).then(async(rows:Match[])=>{const current=rows[0]??null;setMatch(current);if(!current)return;const[lineupRows,goalRows,seasonRows]=await Promise.all([fetch(`${U}/rest/v1/match_lineups?match_id=eq.${id}&select=club_id,formation,manager_name,starting_xi,substitutes`,{headers:H}).then(r=>r.ok?r.json():[]),fetch(`${U}/rest/v1/match_goal_details?match_id=eq.${id}&select=player_id,player_name,player_slug,minute,own_goal,penalty&order=minute.asc`,{headers:H}).then(r=>r.ok?r.json():[]),fetch(`${U}/rest/v1/matches?season_id=eq.${current.season_id}&select=${select}&order=played_on.asc`,{headers:H}).then(r=>r.json())]);setLineups(lineupRows as Lineup[]);setGoals(goalRows as Goal[]);setNearby(seasonRows as Match[])}).catch(()=>{setMatch(null);setLineups([]);setGoals([]);setNearby([])}).finally(()=>setLoading(false))},[id]);
 if(loading)return <section className="matchLoading">Loading match centre…</section>;if(!match)return <section className="archiveContent emptyState"><strong>Match not found.</strong><span>The record may not have been backfilled yet.</span><Link href="/matches/">Browse all matches →</Link></section>;
 const home=match.home?.name??"Unknown",away=match.away?.name??"Unknown",competitionName=match.competition_season?.competition?.name??"Match",competition=match.competition_season?.display_name??competitionName,homeXI=lineups.find(row=>row.club_id===match.home_club_id),awayXI=lineups.find(row=>row.club_id===match.away_club_id),index=nearby.findIndex(row=>row.id===match.id),previous=index>0?nearby[index-1]:null,next=index>=0&&index<nearby.length-1?nearby[index+1]:null,opponent=home.toLowerCase().includes("dunfermline")?away:home;
 return <><section className="mcHero"><div className="mcInner"><div className="meta">{competition.toUpperCase()} · {displayDate(match.played_on).toUpperCase()}</div><div className="scoreStage"><div className="team"><ClubBadge name={home} size="lg"/><h1>{home}</h1></div><div className="score"><span>FULL TIME</span><div><strong>{match.home_score??"–"}</strong><i>:</i><strong>{match.away_score??"–"}</strong></div></div><div className="team"><ClubBadge name={away} size="lg"/><h1>{away}</h1></div></div><div className="facts"><div><span>VENUE</span><b>{match.venue??"Not recorded"}</b></div><div><span>ATTENDANCE</span><b>{match.attendance?.toLocaleString("en-GB")??"Not recorded"}</b></div><div><span>SEASON</span><b>{match.season?.label??"Not recorded"}</b></div><div><span>LINE-UPS</span><b>{lineups.length?"Available":"Being backfilled"}</b></div></div></div></section><nav className="tabs"><a href="#overview">Overview</a><a href="#lineups">Line-ups</a><a href="#context">Explore</a></nav><main className="centre"><section id="overview" className="section"><p className="kicker">01 · MATCH OVERVIEW</p><h2>The result</h2><div className="overview"><ClubBadge name={home} size="md"/><strong>{match.home_score??"–"}</strong><span>FT</span><strong>{match.away_score??"–"}</strong><ClubBadge name={away} size="md"/></div>{goals.length?<div className="scorers"><span>SCORERS</span>{goals.map((goal,i)=>goal.player_slug&&!goal.own_goal?<Link href={`/player/?slug=${goal.player_slug}`} key={`${goal.player_id}-${goal.minute}-${i}`}>{scorerName(goal.player_name??"Unknown scorer")} {goal.minute!=null?`${goal.minute}'`:""}{goal.penalty?" (pen)":""}</Link>:<b key={`${goal.player_id}-${goal.minute}-${i}`}>{scorerName(goal.player_name??"Unknown scorer")} {goal.minute!=null?`${goal.minute}'`:""}{goal.penalty?" (pen)":""}{goal.own_goal?" (og)":""}</b>)}</div>:null}{match.notes?<p className="note">{match.notes}</p>:null}</section><section id="lineups" className="section dark"><p className="kicker">02 · LINE-UPS</p><h2>The teams</h2>{lineups.length?<div className="lineupGrid">{[homeXI,awayXI].map((lineup,i)=>{const clubName=i===0?home:away;if(!lineup)return <article className="sheet empty" key={clubName}><ClubBadge name={clubName} size="md"/><h3>{clubName}</h3><p>Line-up not yet backfilled.</p></article>;return <article className="sheet" key={lineup.club_id}><header><ClubBadge name={clubName} size="md"/><div><h3>{clubName}</h3><p>{lineup.formation??"Formation not recorded"} · Manager: {lineup.manager_name??"Not recorded"}</p></div></header><h4>STARTING XI</h4><div>{lineup.starting_xi.map((player,j)=><PlayerRow player={player} key={`${player.name}-${j}`}/>)}</div><h4>SUBSTITUTES</h4><div className="subs">{lineup.substitutes.map((player,j)=><span key={`${player.name}-${j}`}>{player.name}</span>)}</div></article>})}</div>:<div className="noData"><strong>Line-ups are still being backfilled</strong><p>The match record is valid. Team-sheet detail will appear here when it is added to the archive.</p></div>}</section><section id="context" className="section"><p className="kicker">03 · KEEP EXPLORING</p><h2>Don't stop here.</h2><div className="exploreGrid">{match.season?.slug&&<Link href={`/season/?slug=${match.season.slug}`}><small>SEASON</small><strong>{match.season.label}</strong><span>Tables, squad, transfers and every match →</span></Link>}<Link href={`/head-to-head/?opponent=${encodeURIComponent(opponent)}`}><small>RIVALRY</small><strong>Pars vs {opponent}</strong><span>Open the historical head-to-head record →</span></Link><Link href={`/matches/?competition=${encodeURIComponent(competitionName)}`}><small>COMPETITION</small><strong>{competitionName}</strong><span>Explore matches from this competition →</span></Link><Link href={`/matches/?opponent=${encodeURIComponent(opponent)}`}><small>OPPONENT</small><strong>Every match vs {opponent}</strong><span>Search the full results archive →</span></Link></div><div className="matchPager">{previous?<Link href={`/match/?id=${previous.id}`}>← Previous match</Link>:<span/>}<Link href="/matches/">All matches</Link>{next?<Link href={`/match/?id=${next.id}`}>Next match →</Link>:<span/>}</div></section></main></>;
}
export default function MatchDetail(){return <main className="archivePage"><Suspense fallback={<section className="matchLoading">Loading match centre…</section>}><MatchRecord/></Suspense><style jsx global>{`.mcHero{background:#090a0c;color:#fff;padding:3rem 6vw 0}.mcInner{max-width:1380px;margin:auto}.meta{text-align:center;color:#8b8e94;font-size:.62rem;font-weight:900;letter-spacing:.12em}.scoreStage{display:grid;grid-template-columns:1fr .8fr 1fr;align-items:center;gap:3rem;padding:4rem 0}.team{text-align:center}.team h1{font-size:clamp(1.8rem,3vw,3.3rem);line-height:.95}.score{text-align:center}.score>span{border:1px solid #3b3e44;padding:.4rem .7rem;font-size:.55rem}.score>div{display:flex;justify-content:center;align-items:center;gap:1rem;margin-top:1rem}.score strong{font-size:clamp(5rem,9vw,9rem);line-height:.8}.score i{font-style:normal;color:#d51f2b;font-size:2rem}.facts{display:grid;grid-template-columns:repeat(4,1fr);border-top:1px solid #2d3035}.facts div{text-align:center;padding:1.2rem;border-right:1px solid #2d3035}.facts span{display:block;color:#777;font-size:.5rem;letter-spacing:.12em}.tabs{position:sticky;top:76px;z-index:20;background:#fff;display:flex;justify-content:center;border-bottom:1px solid #ccc}.tabs a{padding:1rem 1.5rem;font-size:.62rem;font-weight:900;text-transform:uppercase}.centre{background:#f2efe7}.section{padding:5rem 6vw;max-width:1400px;margin:auto;scroll-margin-top:130px}.section h2{font-size:clamp(3rem,6vw,6rem);line-height:.9;margin:.5rem 0 2rem}.kicker{font-size:.6rem;color:#d51f2b;font-weight:900;letter-spacing:.14em}.overview{background:#fff;border:1px solid #ccc7bd;padding:2rem;display:flex;align-items:center;justify-content:center;gap:1.5rem}.overview strong{font-size:4rem}.overview span{background:#111;color:#fff;padding:.3rem .5rem;font-size:.55rem}.scorers{background:#fff;border:1px solid #ccc7bd;border-top:0;padding:1.1rem 1.5rem;display:flex;justify-content:center;align-items:center;gap:.85rem;flex-wrap:wrap}.scorers>span{font-size:.52rem;letter-spacing:.12em;color:#d51f2b;font-weight:900}.scorers b,.scorers a{font-size:.76rem;font-weight:800}.scorers a{text-decoration:underline;text-decoration-color:#d51f2b;text-underline-offset:3px}.note{background:#e6e0d5;border-left:4px solid #d51f2b;padding:1rem}.dark{max-width:none;background:#0d0e10;color:#fff}.dark>*{max-width:1220px;margin-left:auto;margin-right:auto}.lineupGrid{display:grid;grid-template-columns:1fr 1fr;gap:1px;background:#333}.sheet{background:#17191c;padding:2rem}.sheet header{display:flex;align-items:center;gap:1rem;border-bottom:1px solid #34363a;padding-bottom:1.2rem}.sheet h4{font-size:.55rem;color:#d51f2b;letter-spacing:.12em;margin:1.5rem 0 .6rem}.playerRow{display:grid;grid-template-columns:36px 1fr auto;padding:.58rem 0;border-bottom:1px solid #292b2f}.subs{display:flex;flex-wrap:wrap;gap:.45rem}.subs span{background:#24262a;padding:.45rem .6rem;font-size:.68rem}.noData{background:#17191c;padding:4rem;text-align:center;color:#888}.exploreGrid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:1px;background:#c9c4ba;border:1px solid #c9c4ba}.exploreGrid>a{background:#fff;color:#111;padding:1.5rem;min-height:150px;display:flex;flex-direction:column;justify-content:space-between}.exploreGrid>a:hover{background:#111;color:#fff}.exploreGrid small{font-size:.52rem;letter-spacing:.12em;color:#d51f2b;font-weight:900}.exploreGrid strong{font-size:1.45rem}.exploreGrid span{font-size:.68rem;color:#777}.exploreGrid>a:hover span{color:#bbb}.matchPager{display:grid;grid-template-columns:1fr auto 1fr;align-items:center;margin-top:1rem}.matchPager a{font-size:.65rem;font-weight:900;text-transform:uppercase;padding:.8rem}.matchPager a:last-child{text-align:right}.matchLoading{padding:8rem;text-align:center;background:#0b0c0e;color:#888;min-height:60vh}.emptyState{display:grid;gap:1rem;justify-items:start;min-height:50vh;padding:6rem}.emptyState a{background:#111;color:#fff;padding:.9rem 1rem;font-size:.65rem;font-weight:900;text-transform:uppercase}@media(max-width:800px){.scoreStage{grid-template-columns:1fr 1fr}.score{grid-column:1/-1;grid-row:1}.facts,.lineupGrid{grid-template-columns:1fr 1fr}}@media(max-width:560px){.facts,.lineupGrid,.exploreGrid{grid-template-columns:1fr}.section{padding:4rem 5vw}.tabs{justify-content:flex-start;overflow:auto}.matchPager{grid-template-columns:1fr 1fr}.matchPager>a:nth-child(2){display:none}}`}</style></main>}
