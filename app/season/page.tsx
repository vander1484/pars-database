"use client";
import Link from "next/link";
import {Suspense,useEffect,useMemo,useState} from "react";
import {useSearchParams} from "next/navigation";
type Season={id:number;label:string;slug:string};type Match={id:number;played_on:string;home_score:number|null;away_score:number|null;home:{name:string}|null;away:{name:string}|null;competition_season:{competition:{name:string}|null}|null};
const U="https://uwhewuwnrcvrnclfzoge.supabase.co",K="sb_publishable_3qBGcpu8I6fytBGxdhJDNA_zOklTBeT",H={apikey:K,Authorization:`Bearer ${K}`};
function Browser(){const q=useSearchParams(),slug=q.get('slug'),[season,setSeason]=useState<Season|null>(null),[matches,setMatches]=useState<Match[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{if(!slug){setLoading(false);return}fetch(`${U}/rest/v1/seasons?slug=eq.${encodeURIComponent(slug)}&select=id,label,slug`,{headers:H}).then(r=>r.json()).then(async s=>{const x=s[0];setSeason(x||null);if(x){const sel='select=id,played_on,home_score,away_score,home:clubs!matches_home_club_id_fkey(name),away:clubs!matches_away_club_id_fkey(name),competition_season:competition_seasons(competition:competitions(name))&order=played_on.asc';setMatches(await fetch(`${U}/rest/v1/matches?season_id=eq.${x.id}&${sel}`,{headers:H}).then(r=>r.json()))}}).finally(()=>setLoading(false))},[slug]);const groups=useMemo(()=>matches.reduce<Record<string,Match[]>>((a,m)=>{const n=m.competition_season?.competition?.name||'Other';(a[n]??=[]).push(m);return a},{}),[matches]);if(loading)return <section className="archiveContent"><p>Loading season…</p></section>;if(!season)return <section className="archiveContent"><h2>Season not found</h2></section>;return <><section className="archiveHero"><p className="eyebrow">SEASON ARCHIVE</p><h1>{season.label.replace('/20','/')}</h1><p>{matches.length?`${matches.length} recorded matches currently connected to the live database.`:'This season is in the migration queue. Match records are being verified before publication.'}</p></section><section className="archiveContent">{Object.entries(groups).map(([c,items])=><section key={c} style={{marginBottom:'4rem'}}><h2>{c} <small>({items.length})</small></h2><div className="dataList">{items.map(m=><Link className="matchRow" href={`/match/?id=${m.id}`} key={m.id}><span>{m.played_on}</span><strong>{m.home?.name} <em>{m.home_score} - {m.away_score}</em> {m.away?.name}</strong><b>→</b></Link>)}</div></section>)}{!matches.length&&<p className="migrationNote">The season is visible in the archive, but no unverified results are being shown.</p>}</section></>}
export default function SeasonPage(){return <main className="archivePage"><header className="archiveNav"><Link className="brand" href="/">PARS<span>DATABASE</span></Link><Link href="/seasons/">← Seasons</Link></header><Suspense fallback={<section className="archiveContent"><p>Loading season…</p></section>}><Browser/></Suspense></main>}
