"use client";
import Link from "next/link";
import {Suspense,useEffect,useMemo,useState} from "react";
import {useSearchParams} from "next/navigation";
const U="https://uwhewuwnrcvrnclfzoge.supabase.co",K="sb_publishable_3qBGcpu8I6fytBGxdhJDNA_zOklTBeT",H={apikey:K,Authorization:`Bearer ${K}`};
type Player={id:number;name:string;slug:string;position:string|null;photo_url:string|null};type Season={id:number;label:string;slug:string};type Match={id:number;played_on:string;home_score:number|null;away_score:number|null;home:{name:string}|null;away:{name:string}|null;competition_season:{competition:{name:string}|null}|null};
function Results(){const p=useSearchParams(),q=(p.get('q')||'').trim(),[players,setPlayers]=useState<Player[]>([]),[seasons,setSeasons]=useState<Season[]>([]),[matches,setMatches]=useState<Match[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{if(!q){setLoading(false);return}const safe=q.replace(/[,*()]/g,' ');Promise.all([fetch(`${U}/rest/v1/players?select=id,name,slug,position,photo_url&name=ilike.*${encodeURIComponent(safe)}*&order=name.asc&limit=30`,{headers:H}).then(r=>r.json()),fetch(`${U}/rest/v1/seasons?select=id,label,slug&label=ilike.*${encodeURIComponent(safe)}*&order=start_year.desc&limit=20`,{headers:H}).then(r=>r.json()),fetch(`${U}/rest/v1/matches?select=id,played_on,home_score,away_score,home:clubs!matches_home_club_id_fkey(name),away:clubs!matches_away_club_id_fkey(name),competition_season:competition_seasons(competition:competitions(name))&or=(home.name.ilike.*${encodeURIComponent(safe)}*,away.name.ilike.*${encodeURIComponent(safe)}*)&order=played_on.desc&limit=40`,{headers:H}).then(r=>r.ok?r.json():[])]).then(([a,b,c])=>{setPlayers(a);setSeasons(b);setMatches(c)}).finally(()=>setLoading(false))},[q]);const total=players.length+seasons.length+matches.length;if(loading)return <p>Searching the archive…</p>;return <>{!q?<div className="emptyState"><strong>Search 100+ years of Pars history.</strong><span>Try a player, season, opponent or competition.</span></div>:<><div className="searchSummary"><strong>{total}</strong><span>results for “{q}”</span></div>{players.length>0&&<section className="searchSection"><h2>Players <small>{players.length}</small></h2><div className="searchCards">{players.map(x=><Link className="searchResult" href={`/players/${x.slug}/`} key={x.id}><span className="playerAvatar">{x.photo_url&&<img src={x.photo_url} alt=""/>}<span className="playerInitials">{x.name.slice(0,2).toUpperCase()}</span></span><span><strong>{x.name}</strong><small>{x.position||'Player'}</small></span><b>→</b></Link>)}</div></section>}{seasons.length>0&&<section className="searchSection"><h2>Seasons <small>{seasons.length}</small></h2><div className="searchCards">{seasons.map(x=><Link className="searchResult" href={`/season/?slug=${x.slug}`} key={x.id}><span className="searchIcon">S</span><span><strong>{x.label}</strong><small>Season archive</small></span><b>→</b></Link>)}</div></section>}{matches.length>0&&<section className="searchSection"><h2>Matches <small>{matches.length}</small></h2><div className="searchCards">{matches.map(x=><Link className="searchResult" href={`/match/?id=${x.id}`} key={x.id}><span className="searchIcon">M</span><span><strong>{x.home?.name} {x.home_score} - {x.away_score} {x.away?.name}</strong><small>{x.played_on} · {x.competition_season?.competition?.name||'Match'}</small></span><b>→</b></Link>)}</div></section>}{total===0&&<div className="emptyState"><strong>No archive records found.</strong><span>Try a shorter player name, a season such as 2016, or an opponent.</span></div>}</>}</>}
export default function SearchPage(){return <main className="archivePage"><section className="compactHero"><p className="eyebrow">SEARCH THE ARCHIVE</p><h1>Find anything.</h1><p>Players, seasons, opponents and matches across Dunfermline Athletic history.</p></section><section className="archiveContent"><Suspense fallback={<p>Searching…</p>}><Results/></Suspense></section></main>}
